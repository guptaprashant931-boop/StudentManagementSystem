# StudentManagementSystem
A role-based Student Management System built using Django and MySQL that allows administrators to manage students, teachers, courses, and attendance efficiently.  This project demonstrates authentication, authorization, CRUD operations, and real-world role management using Django’s built-in features.
